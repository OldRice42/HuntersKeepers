<%= form_with(model: [:hunter, hunters_improvement], local: true) do |form| %>
  <% if hunters_improvement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hunters_improvement.errors.count, "error") %> prohibited this hunters_improvement from being saved:</h2>

      <ul>
      <% hunters_improvement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :improvement %>
    <%= form.collection_select(:improvement_id, @improvements, :id, :description) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
